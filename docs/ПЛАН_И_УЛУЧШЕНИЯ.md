# CyberTeacher - План улучшений и трекер задач

## 📋 Выжимка сессии 2026-03-09

### ✅ Сделано сегодня
- Переход с VL Studio на Ollama (qwen2.5:7b) через subprocess + UTF-8
- Удалены мусорные файлы: `llm.py`, `db_operations.py`
- Промпт учителя вынесен в `config/teacher_prompt.txt`
- Добавлено 20 историй в `config/stories.json` (случайная байка на ответ)
- `question_generation.py` теперь проверяет ответы через LLM (семантически)
- `generators.py` исправлен: добавлены импорты, `extract_json_block`, `ALLOWED_TOPICS`
- `practice.py` добавлена проверка Docker availability
- Создан `.gitignore`, проект закоммичен и запущен на GitHub: https://github.com/Pondag5/CyberTeacher
- Обновлён `docs/СЕССИЯ_2026-03-09.md` полным отчетом
- ✅ **Добавлено кэширование LLM** (in-memory, 100 items) - экономия времени/токенов
- ✅ **Добавлены unit tests** (quiz, news, cache, memory, config) - **8 тестов, все passing**
- ✅ **Добавлена команда `/history`** - показать последние 10 сообщений чата
- ✅ **Добавлена команда `/version`** - показывает версию и git commit
- ✅ **Type hints и docstrings** в новых функциях (`handle_history`, `handle_version`, `OllamaClient`)
- ✅ **Валидация промпта** в `ollama_client.py` (check empty, length limit)
- ✅ **Сгенерирован `requirements.txt`** с актуальными зависимостями
- ✅ **Форматирование кода** через `black` и `isort`

### 🔄 Текущее состояние
- Python 3.12 (работает стабильно)
- LLM: Ollama + qwen2.5:7b
- Русский язык: UTF-8 работает, терминал может показать квадраты (нужен шрифт с UTF-8)
- Квизы: оценка через LLM, fallback на ключевые слова
- Новости: RSS загрузка работает

---

## 💡 Идеи для улучшения (без размышлений, сразу к делу)

### Базовый уровень (быстро и безопасно)
- [x] Добавить кэширование LLM запросов (dict in-memory, 100 items)
- [x] Простые unit tests для `question_generation` и `news_fetcher` (4 теста, all passing)
- [x] Исправить `.gitignore` - исключить `memory/`, `embeddings/`, `*.db`, `*.log`
- [x] Добавить `requirements.txt` с актуальными зависимостями
- [x] Добавить type hints и docstrings в новые функции (`handle_history`, `handle_version`, `OllamaClient`)
- [ ] Привести весь код к единому стилю (black, isort)
- [ ] Добавить обработку ошибок во всех внешних вызовах (Docker, LLM, network)
- [ ] Установить ruff/flake8 и исправить предупреждения
- [ ] Добавить mypy для статической проверки типов

### Средний уровень
6. Реализовать `/clear` команду - очистка чата из БД
7. Добавить `/history` - показать последние сообщения
8. Суммаризация истории: каждые 20 сообщений сворачивать в короткий вывод через LLM
9. Увеличить RAG k=5 + простой реранкинг (по tf-idf)
10. Добавить `/help detail` - подробная справка по каждой команде

### Продвинутый (если есть время/желание)
11. Docker Compose файл для запуска всего стека
12. FastAPI сервер + простой web UI (Gradio)
13. Telegram ботintegration
14. Адаптивный план обучения: если 3 ошибки подряд в теме -> предлагает повторить
15. Экспорт диалога в Markdown (`/export`)

### Безопасность/стабильность
16. Валидация ввода в `practice.py` (docker exec) через ш ponyWhitelist команд
17. Лимит длины ответа LLM (max 2000 симв)
18. Rate limiter: максимум 10 запросов в минуту
19. Логирование ошибок в отдельный файл `errors.log`
20. Graceful degradation если Ollama недоступна - показать сообщение "LLM временно недоступна, попробуйте позже"

### Контент
21. Расширить `QUIZ_TOPICS` (добавить cloud, mobile, iot, blockchain)
22. Добавить реальные CTF задачи из CTFtime.org API
23. Создать `cheatsheets/` папку с one-liners для терминала
24. Добавить `/command <cmd>` - симулятор терминала (безопасный) для отработки команд

### Gamedev (если хотим круто)
25. Система достижений: расширить, добавить секретные
26. Таблица лидеров (top 10 по XP) - текстовый вывод
27. Уровни сложности: easy/medium/hard с подстройкой
28. Недельные челленджи (предлагать раз в неделю сложную задачу)

---

## 🎯 Что делать СЕЙЧАС (приоритет по времени)

1. **Кэширование LLM** - улучшит скорость и экономит токены
2. **Unit tests** - 2-3 теста на quiz и news (10 минут)
3. **Правильный .gitignore** - убедиться что кэши не в Git (уже есть, проверить)
4. **/clear команда** - простая реализация

---

## 📌 known issues

- [ ] LSP errors в IDE (Rich Panel title/border) - не критично
- [ ] Кракозябры в логах - сменить кодировку лог-файла на UTF-8
- [ ] news_fetcher импорт в main.py через try/except - можно стандартизировать

---

*Обновлено: 2026-03-09*
