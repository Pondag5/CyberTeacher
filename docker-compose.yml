version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: cyberteacher-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: cyber_teacher
      POSTGRES_USER: cyberteacher
      POSTGRES_PASSWORD: ${DB_PASSWORD:-cyberteacher_pass}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cyberteacher"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: cyberteacher-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin_pass}
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
  pgadmin_data:
