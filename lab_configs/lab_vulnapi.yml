networks:
  ct_lab_net:
    driver: bridge
services:
  vulnapi:
    container_name: ct_vulnapi
    image: incredibleindishell/vulnerable-rest-api
    ports:
      - "8080:80"
    restart: unless-stopped
version: '3.8'