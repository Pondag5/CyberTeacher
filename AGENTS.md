
## 📄 `AGENT_INSTRUCTIONS.md` (скопируйте в корень проекта)

```markdown
# Инструкции для AI-ассистента (локальная модель)

Ты — AI-ассистент, помогающий разработчику с проектом CyberTeacher. У тебя есть доступ к следующим **инструментам**:

1. **Поиск по кодовой базе (search_codebase)** – через MCP-сервер.
2. **Чтение и редактирование файлов** – через Continue или ручные команды.
3. **Выполнение терминальных команд** – с разрешения пользователя.

**Главное правило:** никогда не полагайся только на свой внутренний контекст. Если вопрос касается конкретных файлов, функций, логики проекта – **обязательно используй search_codebase**, чтобы найти релевантные фрагменты кода или документации.

## Как пользоваться поиском

- Если пользователь спрашивает: «как работает X?» – запусти поиск с запросом `X`.
- Если нужно исправить баг – сначала найди соответствующий код.
- Если пользователь упоминает имя файла или функции – ищи их.
- Всегда **указывай источник** (путь к файлу) в ответе, чтобы разработчик мог проверить.

**Пример вызова search_codebase (в чате Continue):**
```
@code-search search_codebase("функция start_lab")
```

Если MCP-сервер не отвечает, предложи пользователю запустить его вручную:
```bash
python mcp_server_faiss.py
```

## Доступные команды терминала (выполняй с осторожностью)

- `python main.py` – запуск CLI
- `python index_project.py` – обновление векторного индекса
- `python search_faiss.py "запрос"` – ручной поиск (без MCP)
- `python mcp_server_faiss.py` – запуск MCP-сервера

## Структура ответа

Всегда придерживайся формата:

1. **Что нашёл** – перечисли релевантные файлы/функции.
2. **Код или объяснение** – приведи фрагмент (или краткое описание).
3. **Рекомендация** – что делать дальше.

## Запрещено

- Угадывать содержимое файлов, которые не были найдены поиском.
- Предлагать изменения в коде, не убедившись в его актуальности.
- Выполнять опасные команды (удаление файлов, изменение глобальных настроек) без явного подтверждения.

## Память между сессиями

- Твоя память сбрасывается после закрытия чата. Чтобы не потерять контекст:
  - Сохраняй важные решения в файл `DECISIONS.md` (предложи пользователю).
  - При старте новой сессии запроси актуальные `PROJECT_STATE.md` и `CURRENT_TASK.md`.

## Твой главный приоритет

Помочь разработчику **быстро и точно** решить задачу, используя **реальный код из проекта**, а не догадки.
```

<!-- devmemory:managed:start -->
<!-- This block is regenerated by the DevMemory AI VS Code extension. Anything OUTSIDE these markers is preserved on every export. -->
<!-- generated: 2026-09-18T12:00:00.000Z -->

# Project Context (managed by DevMemory AI)

## Project Summary

CyberTeacher — CLI + PWA (Single-File HTML SPA) платформа для обучения кибербезопасности с AI-учителем (Rick/Doctor persona), 8 сюжетными главами (Signal→Convergence), треками, миссиями, risk-механиками (noise, trace, debt, factions), машиной времени (rewind) и атмосферными фичами (echo, phantom labs, secret room, watchers). Стек: Python 3.10+, AI/LLM (LM Studio / Ollama), YAML-конфиги, pytest, векторный индекс FAISS + BM25.

## Architecture

- **main.py** — точка входа CLI + PWA (aiohttp server)
- **handlers/** — 79 хендлеров (story, missions, quiz, htb, labs, practice, tracks, network, rewind, watchers, phantom, secret, etc.)
- **api_server.py** — ~140 REST API endpoints + 3 WebSocket для PWA (FastAPI + aiohttp)
- **state.py** — dataclass-состояние (вся прогрессия студента) с debounced save
- **models/** — Pydantic модели состояния + настроек
- **settings.py** — Pydantic Settings (все пути, LLM, оптимизация)
- **story_mode.py** — 8 глав + 20 эпизодов, интро/аутро, финальный выбор
- **track_engine.py** — треки (YAML) с прогрессией
- **services/** — spaced_repetition_service (SM-2), weak_topics_service, learning_memory_service
- **knowledge.py** — Teacher RAG (FAISS + BM25 + RRF + cross-encoder + query expansion), индексирует knowledge_base/ + docs/
- **static/js/tabs/** — PWA вкладки (51+ .js файлов)
- **tests/** — 85 test файлов, 105+ passed в ключевых модулях
- **story/** — lore/narrative bible (4 файла)
- **docs/STORY_IMPLEMENTATION_PLAN.md** — план развития сюжета (15/15 механик завершены)

## Current State

- **✅** 8 глав (Signal→Convergence), sequential unlock, progress tracking, финальный выбор (3 paths)
- **✅** Risk-механики: noise (0-100), stealth, trace timer, dirty logs, wipe, debts
- **✅** Фракции (ghost_network/archive_division/watchers) + репутация
- **✅** Cyberpsychosis escalation (глитчи в PWA, влияние на учителя)
- **✅** Echo messages (ghost-сообщения прошлых студентов)
- **✅** Teacher memory + Learning events (персонализированные референсы + семантический retrieval)
- **✅** Watchers counterattack (noise ≥70 + dirty logs ≥3 + risk ≥50)
- **✅** Phantom Labs (timed, исчезают навсегда, CP ≥ 30)
- **✅** Secret Room (8 глав + faction + watchers, lore reveal, artifact)
- **✅** Rewind / машина времени (/rewind N, сброс прогресса, потеря памяти, ачивка)
- **✅** Spaced Repetition (SM-2) — исправлен баг с bad review
- **✅** Weak Topics auto-tracking (wired в services/weak_topics_service)
- **✅** Knowledge Base: FAISS + BM25 full-corpus + RRF + query expansion + category/source фильтры + персистентный BM25 индекс
- **✅** State save throttling (2s debounce) + settings.state_file integration
- **✅** Mood/persona система (5 стилей), prompt modifier, personality drift на основе learning events
- **✅** Арх. чистка: удалены secret_language.py, ollama_client.py, checker.py, audit_kb.py, src/, book_new/
- **✅** Тесты: 105+ passed в ключевых модулях (knowledge, quiz, mood, persona, memory, services), ruff 0, mypy 0
- **✅** **Оптимизации импортов:** startup ↓10× (10s → 1s), знание конфига ↓44× (7.7s → 173ms)
- **✅** GitHub repo description и topics обновлены
- **⏳** PWA: риск-индикаторы, rewind UI, secret room status
- **⏳** Backdoors — низкий приоритет
- **❌** Shadow Teacher, Digital Grave, Sacrifice, Doppelganger — CUT per plan

## Next Actions

1. **PWA:** риск-индикаторы (noise bar, trace progress, debt counter) — частично
2. **Knowledge Base:** CLI/API команда принудительной переиндексации
3. **P3 Long-term:** Mobile, OAuth2, 2FA

<!-- devmemory:managed:end -->
